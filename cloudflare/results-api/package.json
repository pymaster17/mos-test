{
  "name": "mos-results-api",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:migrate": "wrangler d1 migrations apply RESULTS_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
